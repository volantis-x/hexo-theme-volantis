<script>
  function load_swiper() {
    if (!document.querySelector(".swiper-container")) return;
    volantis.css("<%- theme.cdn.swiper_css %>");
    volantis.js("<%- theme.cdn.swiper_js %>").then(() => {
      pjax_swiper();
    });
  }

  load_swiper();

  function pjax_swiper() {
    const effect = document.querySelector(".swiper-container").getAttribute('effect') || 'cards';
    volantis.swiper = new Swiper('.swiper-container', {
      slidesPerView: 'auto',
      spaceBetween: 8,
      centeredSlides: true,
      effect: effect,
      rewind: true,
      pagination: {
        el: '.swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
      },
    });
  }

  volantis.pjax.push(() => {
    if (!document.querySelectorAll(".swiper-container")[0]) return;
    if (typeof volantis.swiper === "undefined") {
      load_swiper();
    } else {
      pjax_swiper();
    }
  },"swiper");
</script>
