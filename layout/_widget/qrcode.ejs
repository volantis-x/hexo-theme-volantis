<%- partial('_pre') %>
<section class="widget <%- widget_style %>">
  <%- partial('header', {item: item}) %>
  <div class='content article-entry'>
    <% getList(item.images).forEach(function(url){ %>
      <img alt="qrcode" src='<%- url_for(url) %>'
      <% if (item.height) { %>
        height='<%- item.height %>'
      <% } %>/>
    <% }) %>
  </div>
</section>
